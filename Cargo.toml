[package]
name = "lau-protocol"
version = "0.1.0"
edition = "2021"
description = "Layered Agent-UI protocol — bridges PLATO room agents to external game engines and UIs"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
